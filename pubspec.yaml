name: hb_monitor_app
description: HemePulse BLE monitoring companion app.
publish_to: none
version: 2.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_blue_plus: ^1.35.5
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  permission_handler: ^12.0.1
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
